"use strict";


// Establish the starting values that we "know"
var fahrenheitTemp = 80
var celcius;

//Calculate any values that we dont already know, but that can be figured out
var celcius = 5/9 * (fahrenheitTemp - 32);

//Display the results

var message = "The Fehrenheit Temp: " + fahrenheit " is the same as the Celcius Temp: " + celcius;
var message = `The Fehrenheit Temp: ${fahrenheit} is the same as the Celcius Temp: ${celcius}`;

console.log(message);




/* 
Establish the starting values that we "know"
let fahrenheit;
let celsius;

calculate any values that we dont
already know, but that we cant figure out

let fahrenheit = 98
let celsius = 5/9 * (fahrenheit - 32);


display the results.

var message = "the fahrenhit temp" + fahrenheit + "is the same as the celsius temp " + celsius

console.log(celsius); */